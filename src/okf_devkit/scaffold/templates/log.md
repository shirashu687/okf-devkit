# 変更履歴 — <層名>

> OKF v0.2 §9 準拠。日付見出し（`YYYY-MM-DD`）でグループ化し、**新しい順**に並べる。
> プレフィックスは `**Creation**` / `**Update**` / `**Deprecation**` の3種のみ。不具合修正は `**Update**` に含める。
> 行末のコミットハッシュは `okf log` が既出判定に使うため削除しないこと。
> 通常は `okf log --write` で自動追記される。

## <YYYY-MM-DD>
- **Creation** <新規追加した機能の説明を散文で1〜2文。> (`<hash>`)
- **Update** <既存の変更・不具合修正の説明。> (`<hash>`)
- **Deprecation** <削除・廃止した内容の説明。> (`<hash>`)
